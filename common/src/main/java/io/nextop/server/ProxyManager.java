package io.nextop.server;

public class ProxyManager {

    // take Observable<NextopSession>
    // on each connect to an existing ProxySession (using the clientId in the NextopSession),
    //     or create a new ProxySession



}
