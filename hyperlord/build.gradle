
mainClassName = "io.nextop.service.hyperlord.HyperlordService"
applicationDefaultJvmArgs = []

sourceSets {
    main {
        java {
            srcDir 'src/main/java'
        }
        resources {
            srcDir 'src/main/resources'
        }
    }
}

dependencies {
    compile project(':common')
    compile project(':schema')
    compile project(':nextop-client:java')
}
